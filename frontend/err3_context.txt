                    // Categoriakdown
                    const cat = t.Categoriategoria || 'Sin Categoria';
                    const amount = parseFloat(t.amount || t.monto || t.Monto || 0);

                    if (!Categoriat]) Categoriat] = 0;
                    Categoriat] += amount;

                // Timeline (cumulative)
                const date = (t.date || t.fecha || t.Fecha || '').split('T')[0];
                if (date) {